<UserControl x:Class="LightflowStudio.VisualIndexView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:LightflowStudio"
 AutomationProperties.Name="Visual Index">
 <Grid Margin="8">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <DockPanel LastChildFill="False">
   <TextBlock Text="Frames" VerticalAlignment="Center" Margin="0,0,8,0"/>
   <ComboBox x:Name="Density" Width="76" SelectionChanged="Density_SelectionChanged" AutomationProperties.Name="Visual Index frame count"/>
  </DockPanel>
  <TextBlock x:Name="GenerationStatus" Grid.Row="1" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" Margin="0,6,0,8"/>
  <TextBlock x:Name="EmptyState" Grid.Row="2" Text="Waiting for video duration…" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}"/>
  <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" SizeChanged="Grid_SizeChanged">
   <ItemsControl x:Name="Frames" Loaded="Frames_Loaded">
    <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate><DataTemplate>
     <Button Click="Frame_Click" Margin="0,0,6,8" Padding="5" HorizontalContentAlignment="Stretch"
      ToolTip="{Binding NavigationLabel}" AutomationProperties.Name="{Binding NavigationLabel}">
      <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
       <Setter Property="Background" Value="#15181E"/><Setter Property="BorderBrush" Value="#292E37"/>
       <Style.Triggers><DataTrigger Binding="{Binding IsCurrent}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource OrangeBrush}"/></DataTrigger></Style.Triggers>
      </Style></Button.Style>
      <Grid Width="{Binding FrameWidth, RelativeSource={RelativeSource AncestorType=UserControl}}">
       <Grid.RowDefinitions><RowDefinition Height="{Binding FrameHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
       <TextBlock Text="{Binding Status}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}"/>
       <local:OrientedPreviewImage AssetId="{Binding AssetId}" Source="{Binding Frame}" Stretch="Uniform"/>
       <DockPanel Grid.Row="1" Margin="0,5,0,0">
        <TextBlock Text="{Binding CurrentLabel}" DockPanel.Dock="Right" FontSize="10" Foreground="{DynamicResource MutedTextBrush}" Margin="4,0,0,0"/>
        <TextBlock Text="{Binding Timestamp}" FontSize="11"/>
       </DockPanel>
      </Grid>
     </Button>
    </DataTemplate></ItemsControl.ItemTemplate>
   </ItemsControl>
  </ScrollViewer>
 </Grid>
</UserControl>
